// talkback.c -- nosy, informative program
#include <stdio.h>
#define SQUARES 64

int main(void) {
       
     int top, score;
     top = score = -(2 + 5) * 6 + (4 + 3 * (2 + 3));
     printf("Top = %d, score = %d\n", top, score);
     return 0;
}